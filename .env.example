TASK_INSIGHTS_MAX_LIST_ITEMS=100

# Off by default. Set to true to turn on this app's own Rails.logger.debug
# tracing (request param dumps, LLM prompt/response dumps, ...) without
# touching Rails' framework-wide log_level (which would also reactivate
# unrelated debug noise like SQL logging and ActionView partial renders).
APP_DEBUG_LOGGING=false
