<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900"><%= t('views.task_categories.form.edit_category') %></h1>
  </div>

  <%= render 'form', task_category: @task_category %>
</div>
