<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900"><%= t('views.statuses.form.new_status') %></h1>
  </div>

  <%= render 'form', status: @status, form_url: project_statuses_path(@project) %>
</div>
